{
  "name": "girin-codex-notion-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "openapi.yaml -> Notion API 명세 DB 결정적 동기화 (AI 미사용)",
  "scripts": {
    "sync": "node sync.mjs",
    "sync:dry": "node sync.mjs --dry-run",
    "sync:apply": "node sync.mjs --apply"
  },
  "dependencies": {
    "@apidevtools/swagger-parser": "^10.1.0",
    "@notionhq/client": "^2.2.15"
  }
}
