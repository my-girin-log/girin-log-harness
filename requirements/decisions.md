# 요구사항 결정 이력

요구사항 변경은 날짜 / 결정 / 이유 / 영향 범위를 남긴다.

| 날짜 | 결정 | 이유 | 영향 범위 |
| --- | --- | --- | --- |
| 2026-06-05 | 기존 통합 기획안에서 MVP 범위를 추렸다. | 핵심 회고 생성 흐름에 집중하기 위해서. | `requirements/product.md`, `domain/data-model.md`, `api/openapi.yaml` |
| 2026-06-06 | MemoSummary 및 작업 공간 구조를 MVP 흐름에 반영했다. | Memo 전체를 바로 대화로 넘기기보다 카테고리별 요약을 선택해 대화하는 흐름이 더 명확하기 때문. | `Memo`, `MemoSummary`, `DailyChatSession`, `Diary` |
| 2026-06-08 | 별도 ChatMessage 엔티티를 두지 않고 DailyChatSession에 전체 대화 원문을 저장한다. | MVP에서는 메시지 단위 저장보다 하나의 대화 세션 전체를 보관하는 모델이 더 단순하고 충분하기 때문. | 요구사항, 데이터 모델 |
| 2026-06-08 | Diary는 하루의 DailyChatSession들을 기반으로 생성한다. | 사용자가 하루를 어떻게 보냈는지 요약하는 날짜별 정리본이므로 대화 세션 전체 맥락만 사용하기 때문. | 요구사항, 데이터 모델, Diary 생성 정책 |
| 2026-06-08 | Retrospective는 선택 기간의 DailyChatSession들과 persona.md를 기반으로 생성한다. | 회고는 기간 내 대화 원문과 누적 Persona 신호를 사용해 사용자다운 완성형 글을 만드는 것이 목적이기 때문. | 요구사항, 데이터 모델, Retrospective 생성 정책 |
| 2026-06-06 | Diary는 06:00 KST 자동 생성만 MVP 범위로 둔다. | 초기 MVP에서는 수동 생성보다 일일 자동 정리 흐름을 우선 검증하기 위해서. | Diary API, 배치/스케줄러, 테스트 |
| 2026-06-06 | Export 서버 API는 MVP에서 만들지 않는다. | Markdown 복사/다운로드는 FE에서 처리할 수 있기 때문. | API 목록, FE 기능 요구사항 |
| 2026-06-06 | EventLog는 MVP에서 얇은 append-only 로그로 실제 저장한다. | 사용자 행동 기반 MVP 검증 지표를 초기부터 수집하기 위해서. | DB 모델, 이벤트 저장 로직 |
| 2026-06-06 | 백엔드 기본 DB는 PostgreSQL 우선으로 둔다. | 관계형 데이터와 긴 텍스트, 유연한 JSON 메타데이터를 함께 다루기 좋기 때문. | 백엔드 인프라, JPA 매핑, Testcontainers |
| 2026-06-09 | **MemoSummary는 한 번 대화에 사용되면 비활성화하고 재선택을 금지한다.** 별도 수정/삭제 API는 두지 않으며, 목록 응답에서 대화 가능 여부를 내려준다. | 사용자가 이미 대화한 카테고리를 다시 선택하면 같은 맥락의 대화가 중복되고 Diary/Retrospective 입력이 흐려지기 때문. | `MemoSummary`, `DailyChatSession`, `api/openapi.yaml`, `conventions/api.md`, `domain/data-model.md` |
| 2026-06-09 | **Memo 요약은 전체성공/전체실패(원자적).** 실패 시 Memo는 `DRAFT` 유지. 대상 DRAFT가 없으면 `422`(`NO_SUMMARIZABLE_MEMO`). | 부분 성공은 FE UX와 상태 추론을 복잡하게 만들고, 형식은 맞고 규칙 위반인 케이스는 422가 맞기 때문. | `api/openapi.yaml`(요약 422), `conventions/api.md` |
| 2026-06-09 | **MemoSummaryItem.memoId를 API 응답에서 제외**(서버 내부 추적용으로만 유지). | 프론트는 원본 Memo 연결이 필요 없고, 응답은 노출 최소값(id·categoryName·itemCount·items.content)으로 충분하기 때문. | `api/openapi.yaml`(MemoSummaryItem), `domain/data-model.md` |
| 2026-06-09 | **06:00 KST에 OPEN 세션을 자동 종료**한다(`endedReason=SYSTEM_ENDED` 신설). DRAFT Memo는 `ARCHIVED` 전환. | Diary 입력(전날 ENDED 세션)을 확정적으로 만들고 미처리 데이터의 상태를 명확히 하기 위해서. | `EndedReason` enum, `conventions/api.md`, `domain/data-model.md`, 배치/스케줄러 |
| 2026-06-09 | **DailyChatSession은 하나의 `serviceDate`에만 속한다**(생성 시점 기준). 다른 날짜 MemoSummary 혼합 금지(`422`). | Diary 날짜별 집계를 단순·명확하게 하기 위해서. | `conventions/api.md`, `domain/data-model.md` |
| 2026-06-09 | **세션 시작 시 선택 MemoSummary 내용을 snapshot으로 저장**(`selectedSummariesSnapshot`, jsonb). | 원본 상태/내용 변화와 무관하게 대화 기록의 맥락을 보존하기 위해서. | `domain/data-model.md`, 영속성 |
| 2026-06-09 | **ConversationTurn에 `createdAt`(선택) 추가, `sequence`는 두지 않는다.** | 순서는 배열 순서가 계약이므로 sequence는 중복이고, 시간 표시 대비 createdAt만 선택 제공하면 충분하기 때문. | `api/openapi.yaml`(ConversationTurn) |
| 2026-06-09 | **Diary는 대화가 있는 날만 생성**(빈 Diary 금지, 없는 날 404). 재생성은 **멱등**. | 날짜당 1개 unique 제약과 배치 재실행 안전성을 위해서. | `conventions/api.md`, Diary 생성 배치 |
| 2026-06-09 | **회고 생성 입력 = 선택 기간의 `DailyChatSession.conversation` + `persona.md`.** 원본 `Memo`, `MemoSummary`, `Diary`는 직접 입력이 아니다. 기간 기준은 `serviceDate`. **2026-07-09 결정으로 대체됨.** | 당시에는 회고 생성은 기간 내 대화 원문과 사용자다운 말투 기준을 결합하는 흐름으로 단순화하고, Diary는 날짜별 조회용 요약으로 역할을 분리하기 위해서였다. | `requirements/product.md`, `conventions/api.md`, `domain/data-model.md`, `api/openapi.yaml`, Retrospective 생성 |
| 2026-06-09 | **달력용 경량 엔드포인트 `GET /api/diaries/calendar`를 추가**한다(Diary 있는 `serviceDate` 목록만, 본문 제외). 없는 날짜는 `404` 유지. | full Diary 목록을 달력에 쓰면 본문까지 과하게 전송되기 때문. | `api/openapi.yaml`(diaries calendar), FE 달력 |
| 2026-06-09 | **Persona는 온보딩으로 초기 생성하고, 이후 사용자 기록을 바탕으로 주기적으로 갱신한다.** 매일 갱신으로 고정하지는 않으며, 회고 생성 시 persona가 없거나 오래돼도 **차단하지 않고 graceful**하게 진행한다. 노출용 Persona와 내부 `markdown`(persona.md)은 분리 유지한다. **2026-07-13 결정으로 영역별 갱신 책임이 구체화됨.** | 사용자의 말투와 회고 기준은 기록이 쌓이며 변하므로 persona.md도 사용자 맞춤 기준으로 함께 변화해야 하기 때문. 다만 갱신 주기와 트리거는 비용·품질을 보며 조정한다. | `requirements/product.md`, `domain/data-model.md`, `conventions/api.md`, Persona 갱신 정책 |
| 2026-06-09 | **스트릭은 MVP에서 별도 API/스키마로 두지 않는다.** 필요 시 `EventLog.CHAT_SESSION_STARTED`에서 파생(같은 `serviceDate`는 1일 1회). | Pet/스트릭은 MVP 제외 도메인이고, 표시가 필요해지면 이미 쌓는 EventLog로 충분히 계산되기 때문. | `domain/data-model.md`(EventLog), (스키마 변경 없음) |
| 2026-06-10 | **온보딩 재제출을 허용한다.** Persona는 사용자당 1개, 별도 버전 없음. `OnboardingSurvey`·`PersonaSource`는 누적. 차단(`409`/`403`) 없음, `onboardingCompleted`는 true 유지. **2026-07-13 결정에 따라 재제출은 Persona 전체 덮어쓰기가 아니라 Explicit Preferences 교체와 Effective Guidelines 재계산을 뜻한다.** | 재온보딩을 막기보다 갱신이 자연스럽고(Persona 주기 갱신 방향과 일치), 버전 관리는 MVP에 과하기 때문. | `conventions/api.md`, `domain/data-model.md`, Onboarding 구현 |
| 2026-06-10 | **EventLog는 백엔드 내부에서만 자동 기록한다.** 별도 생성 API를 두지 않고, 프론트 클릭/노출 이벤트는 MVP에서 수집하지 않는다. | 후보 이벤트가 모두 서버 도메인 동작이라 내부 기록이 단순·정확하고, FE 행동 수집은 MVP 검증 범위 밖이기 때문. | `domain/data-model.md`(EventLog), `conventions/api.md`, (API 추가 없음) |
| 2026-06-10 | **온보딩 설문 문항은 MVP에서 고정.** `questionId↔문구` SSOT는 `requirements/product.md` 5절. FE 하드코딩 허용, **문항 조회 API 없음**. | MVP에서 문항이 고정이라 API로 내릴 필요가 없고, 번호 매핑만 product.md로 단일화하면 FE 드리프트를 막을 수 있기 때문. 동적화되면 `GET /api/onboarding/questions` 후속. | `requirements/product.md`(SSOT), `conventions/api.md` |
| 2026-06-18 | **스트릭(잔디)을 별도 API/스키마(`StreakDay`)로 도입하고, 달성 기준을 "채팅 세션 종료(ENDED)"로 한다.** (2026-06-09 "별도 API/스키마로 두지 않는다" 결정을 대체) 해당 `serviceDate`에 종료된 `DailyChatSession`이 있으면 그날 스트릭 1개를 기록한다(하루 1회, 종료 사유 무관). `StreakDay.diaryId`는 nullable이며 세션 기준에선 보통 비어있다. | FE와의 합의가 "채팅을 종료하면 스트릭을 기록"이고, 잔디 위젯/요약을 위해 EventLog 파생보다 전용 스키마·조회 API가 단순·명확하기 때문. | `api/openapi.yaml`(TodayStreakResponse·StreakDay·streaks), `domain/data-model.md`(StreakDay), `backend`(DailyChatSessionService·StreakService) |
| 2026-06-18 | **계약 표기 정합화.** ① API 버전 prefix는 `/api`로 통일(`/api/v1` 아님, openapi 실제 경로 기준). ② `DailyChatSession` 응답에 `serviceDate`(date, required) 포함. ③ 온보딩 문항 수 SSOT는 `product.md` 5절(현재 8문항), "약 10개"는 코드 검증 허용폭(1~10)일 뿐 계약 수치가 아님. | 문서·코드·계약 간 표기 불일치(FE 피드백)를 실제 구현 기준으로 맞추기 위해서. | `conventions/api.md`, `api/openapi.yaml`(DailyChatSession), `requirements/product.md` |
| 2026-07-09 | **Diary 생성 입력을 Memo 원본 + DailyChatSession으로 확장하고, MemoSummary는 있을 때만 보조 힌트로 참고한다. Retrospective 생성 입력은 선택 기간의 Diary + DailyChatSession.conversation + persona.md로 바꾼다.** 원본 Memo는 Diary에 흡수된 하루 맥락으로 사용하고, MemoSummary는 Diary 생성의 카테고리/압축 힌트로만 사용한다. Retrospective 생성에는 원본 Memo와 MemoSummary를 기본 직접 입력으로 다시 넣지 않는다. **2026-07-12 결정으로 희소 기간의 입력 선택·최소 입력 조건이 보완됨.** | 채팅하지 않은 Memo가 Diary/Retrospective 산출물에 반영되지 않아 사용자 입장에서는 기록이 사라진 것처럼 보이는 QA 문제가 있었기 때문. Diary가 하루 전체 기록을 보존하고, Retrospective는 Diary로 전체 맥락을 받은 뒤 대화 원문으로 깊어진 회고 신호를 보강하는 구조가 중복과 토큰 비용을 줄인다. MemoSummary는 AI가 한 번 요약한 결과라 원본과 동등한 사실 근거로 쓰면 중복·요약 편향이 생길 수 있으므로 보조 힌트로 제한한다. | `requirements/product.md`, `requirements/scenarios.md`, `domain/data-model.md`, `conventions/api.md`, `api/openapi.yaml`, Diary/Retrospective 생성기 |
| 2026-07-09 | **Memo 수정/삭제를 MVP 범위에 추가하되 `DRAFT` Memo로 제한한다.** `SUMMARIZED`/`ARCHIVED` Memo 수정·삭제는 거부하고, 삭제된 Memo는 조회·요약·Diary 생성 입력에서 제외한다. | 사용자가 요약 전 작성 실수를 바로잡거나 불필요한 기록을 제거할 수 있어야 하지만, 이미 MemoSummary나 Diary/Retrospective 근거가 된 기록을 바꾸면 생성 결과의 추적성이 깨지기 때문이다. | `requirements/product.md`, `requirements/scenarios.md`, `domain/data-model.md`, `conventions/api.md`, `api/openapi.yaml`, Memo 구현 |
| 2026-07-11 | **하루 완료/스트릭 달성 기준을 "채팅 세션 종료"에서 "해당 서비스 날짜의 Memo 작성"으로 변경한다.** 내용 있는 Memo를 작성하면 그날 스트릭 1개를 기록한다(하루 1회). 잔디 칸은 작성 여부만 표시하며 작성 개수에 따른 명암 차이는 두지 않는다. | 사용자가 메모만 남기고 종료하는 경우도 기록 보존 비용과 사용자 가치가 크기 때문이다. 메모 작성 자체를 하루 완료로 인정해야 "메모만 남긴 날"이 사라진 것처럼 느껴지지 않는다. | `requirements/product.md`, `domain/data-model.md`, `api/openapi.yaml` |
| 2026-07-11 | **당일 회고 생성을 위해 현재 진행 중인 serviceDate는 Diary 대신 임시 DailyContext를 사용할 수 있다.** 선택 기간에 현재 serviceDate가 포함되어 있고 아직 Diary가 없다면, 당일 Memo 원본을 Retrospective 생성 시점의 스냅샷으로 묶어 입력한다. DailyChatSession 원문은 DailyContext에 포함하지 않고 기간 단위 직접 입력으로 사용한다. 이 DailyContext는 저장하지 않고 정식 Diary를 대체하지 않으며, 다음 06:00 KST에는 기존 자동 생성 흐름대로 Diary를 생성한다. **2026-07-12 결정으로 내용 있는 Memo가 있을 때만 구성하도록 조건이 보완됨.** | 사용자가 오늘 작성한 내용을 다음 날까지 기다리지 않고 회고에 반영할 수 있어야 하기 때문이다. 오늘 Diary를 수동 생성해 확정해버리면 이후 당일 기록 포함 여부가 애매해지므로, 회고 생성기에만 live 입력을 허용한다. DailyChatSession 원문을 별도 입력으로 유지해 DailyContext와의 중복을 막는다. | `requirements/product.md`, `requirements/scenarios.md`, `domain/data-model.md`, `conventions/api.md`, `api/openapi.yaml`, Retrospective 생성 |
| 2026-07-12 | **선택 기간의 기록 없는 날짜는 과거·현재·미래와 관계없이 건너뛰고 Retrospective를 생성한다.** 요청 기간 메타데이터는 유지하며, 현재 serviceDate에 Diary가 없고 내용 있는 Memo가 있을 때만 Memo 원본으로 비영속 DailyContext를 만든다. 기간 내 DailyChatSession 원문은 DailyContext에 포함하지 않고 Diary 유무와 관계없이 직접 입력하며, 전체 기간에 Diary·DailyContext·DailyChatSession 중 하나도 없을 때만 `422`(`NO_RETROSPECTIVE_SOURCE`)로 거부한다. | 사용자가 중간에 기록을 빼먹은 날 때문에 기간 전체 회고 생성이 막히는 것보다, 실제로 남아 있는 기록만으로 선택 기간을 회고하는 흐름이 자연스럽기 때문이다. persona.md나 공백 Memo만으로 근거 없는 회고가 생성되는 것은 막는다. | `requirements/product.md`, `requirements/scenarios.md`, `domain/data-model.md`, `conventions/glossary.md`, `conventions/api.md`, `api/openapi.yaml`, Retrospective 생성 |
| 2026-07-13 | **내부 persona.md를 Explicit Preferences, Observed Traits, Effective Guidelines로 분리한다.** 설문 수정은 Explicit Preferences만 교체하고 Observed Traits를 보존한다. 기록 기반 갱신은 온보딩 원천에서 얻은 기존 경향을 유지하면서 기존 Observed Traits와 확정 Diary·ENDED DailyChatSession을 함께 고려해 보강·정제한다. 두 경우 모두 Effective Guidelines를 다시 계산하며 최신 명시적 의사를 우선하되 관찰된 차이를 삭제하지 않는다. | 설문 수정이나 새 기록만으로 블로그·기존 글·사용자 기록에서 축적한 말투와 회고 성향이 유실되는 것을 막고, 각 원천의 갱신 책임을 명확히 하기 위해서다. | `requirements/product.md`, `domain/data-model.md`, `conventions/glossary.md`, `conventions/api.md`, `api/openapi.yaml`, Persona·Onboarding·Retrospective 구현 |
| 2026-07-13 | **Persona 세부 정책을 확정한다.** ① Memo는 확정 원천에서 확인된 경향의 보조 근거로만 사용, ② 공개 `Persona.markdown` 제거, ③ 설문 반영 시각은 `OnboardingSurvey.submittedAt` 사용, ④ OnboardingSurvey·PersonaSource는 계정 생명주기와 함께 보존·삭제, ⑤ 블로그 URL은 HTTP(S)만 허용하고 redirect별 DNS/IP 재검증·내부 주소 차단·fetch 상한을 강제한다. | 내부 Persona 노출을 막고, 미완성 기록의 과대 학습·원천 이력의 불명확한 보존·URL 분석의 SSRF 위험을 방지하면서 기존 시각 모델을 유지하기 위해서다. | `requirements/product.md`, `requirements/scenarios.md`, `domain/data-model.md`, `conventions/api.md`, `api/openapi.yaml`, Persona·Onboarding 구현 |
